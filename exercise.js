function canPlay() {
  let sport = " Football";

  if (true) {
    let personName = "Cosimo";
    console.log(personName + sport);
  }

  //console.log(personName + sport);
}

canPlay();

/* This is a function scope, 
variables declare inside a
function is not accessible
 from outside the function,
 I solve the function to run,
 but I didn't
 understand well, So I will
 continue to study to
 understand better*/
